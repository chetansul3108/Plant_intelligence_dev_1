sap.ui.define([
    "sap/ui/core/UIComponent"
], function (UIComponent) {

    "use strict";

    return UIComponent.extend(
        "plant_intelligence_dev.Component",
        {
            metadata: {
                manifest: "json"
            }
        }
    );
});